<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="personal.project.doculysis">

    <dist:module dist:instant="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".operation_genre.genre_option"/>
        <activity android:name=".getFile_g"/>
        <activity android:name=".operation_genre.getText_g"/>
        <activity android:name=".operation_genre.getURL_g"/>

        <activity android:name=".operations_summary.summary_option"/>
        <activity android:name=".getSummaryFromFile"/>
        <activity android:name=".operations_summary.getSummaryFromText"/>
        <activity android:name=".operations_summary.getSummaryFromurl"/>
        <activity android:name=".readappkication.Summary"/>


    </application>

</manifest>